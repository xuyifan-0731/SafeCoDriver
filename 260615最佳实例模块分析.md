# 260615 最佳实例模块分析

## 1. 选择的实例

本文选择 `SUMO stress / BS3_blind_stress` 作为最佳解释实例。

选择原因：这个场景最能体现最终方法 `Hybrid+AND+TTC+RearEscape-thr0.30` 的独特价值。它不是一个“所有方法都容易成功”的简单场景，而是多数方法都会失败：

- 无控制方法撞上前方/侧向攻击车。
- RSS、RiskMM、普通 Hybrid、AND+TTC 会因为制动或过度减速，被后方攻击车追尾。
- MinHarm 能降低严重度，但仍发生后车碰撞。
- 只有最终 `RearEscape` 同时避免前向碰撞和后向追尾，实现 `0` 真实碰撞、`0` overlap、`0` waypoint collision。

该实例结果来自：

- `results/260520_sumo_stress_final/per_run.csv`
- `results/260520_sumo_stress_final/summary.csv`

## 2. 场景定义

`BS3_blind_stress` 是一个路口转角遮挡压力场景。

基础 BS3 场景由 `experiments/gen_scenario_gifs.py:111` 定义：

- ego 从一条道路进入路口，初始速度约 `12.0 m/s`。
- attacker 从交叉方向进入路口，初始速度约 `11.5 m/s`。
- `blk_parallel` 与 attacker 并排行驶，形成遮挡。
- blind 版本中没有有效 coop 车直接帮助 ego 看到所有风险，而是以遮挡/盲区为主要困难。

stress 版本由 `experiments/run_modified_sumo_comparison.py:746` 附近的 `inject_rear_attacker()` 注入后方攻击车 `attacker_rear`。在 `apply_scene_controls()` 中，非随机 stress 后车速度被固定为 `13.5 m/s`，见 `experiments/run_modified_sumo_comparison.py:396`。这意味着如果 ego 因前方风险急刹，就很容易被后车追尾。

该场景的核心难点是：

```text
前方/侧方有冲突：
  如果不处理，ego 在 step 49 左右撞上 attacker。

后方有高速追车：
  如果处理方式是急刹，ego 会在 step 19-35 左右被 attacker_rear 追尾。

因此，正确策略不是简单“看见风险就刹车”，而是需要判断风险方向并选择横向逃逸。
```

## 3. 关键结果对比

`BS3_blind_stress` 单场景 per-run 结果如下。

| 方法 | 是否碰撞 | 首次碰撞 step | 碰撞对象 | severity | first warning | WPC | warning frames | 结论 |
|---|---:|---:|---|---:|---:|---:|---:|---|
| NoCon-egoonly | 1 | 49 | attacker+ego | 2.00 | -1 | 0/870 | 0 | 不处理，撞前方 attacker |
| RSS-coop | 1 | 22 | attacker_rear+ego | 13.50 | 11 | 6/2200 | 56 | 过早/过强制动，被后车追尾 |
| RiskMM-coop | 1 | 20 | attacker_rear+ego | 13.47 | 11 | 6/2200 | 83 | 风险响应强，但诱发后向碰撞 |
| Hybrid-thr0.30 | 1 | 19 | attacker_rear+ego | 13.50 | 11 | 0/2200 | 57 | 路点安全，但速度策略导致追尾 |
| Hybrid+AND+TTC | 1 | 35 | attacker_rear+ego | 13.50 | 27 | 0/2200 | 36 | 误报少一些，但仍因减速被追尾 |
| RearAware | 1 | 35 | attacker_rear+ego | 13.39 | 27 | 0/2200 | 35 | 缓解严重度有限，仍未脱离后车 |
| MinHarm | 1 | 46 | attacker_rear+ego | 11.60 | 27 | 0/2200 | 26 | 延后碰撞并降低严重度，但仍碰撞 |
| RearEscape | 0 | -1 | 无 | 0.00 | 26 | 0/2200 | 40 | 同时避开前向和后向风险 |

从这个表可以看出，最终方法的优势不是单一检测精度，而是完整决策链：

```text
几何约束保证 WPC = 0/2200；
AND+TTC 保证危险及时触发；
RearEscape 避免把前向风险转化成后向追尾；
SUMO 闭环横向逃逸最终让真实碰撞 = 0。
```

## 4. 每个模块做了什么、得到什么结果

### 4.1 模块一：协同/遮挡感知输入构造

代码入口：`experiments/run_modified_sumo_comparison.py:305`

该模块从 SUMO 中读取 ego、attacker、blocker、rear attacker 等车辆状态，并把它们转换成 ego 坐标系下的 `PerceptionResult`。

它做了三件事：

1. 判断目标是否在 ego 感知范围内。
2. 用 `blk_*` 车辆做 LOS 遮挡判断。
3. 如果启用协同感知，则允许 coop 范围内目标进入感知结果；否则只保留 ego 可见目标。

输出形式是：

```text
PerceptionResult(
  ego = ego state,
  agents = [
    attacker / blocker / attacker_rear / ...
  ],
  each agent has:
    relative x, y,
    relative vx, vy,
    length, width,
    is_visible,
    confidence
)
```

在 `BS3_blind_stress` 中，这一步给后续模块提供了两个关键信息：

- 前方/侧方 attacker 正在形成路口冲突。
- 后方 `attacker_rear` 正在接近 ego。

如果没有这个输入，后续模块只能做到“看见前方危险就刹车”，无法知道“刹车会导致后车追尾”。

### 4.2 模块二：可见性和接近速度自适应安全半径

代码入口：`coop_safety/learned/hybrid_safety.py:91`

该模块为每个 agent 生成动态安全半径：

```text
visible target:   base margin = 2.5 m
V2X-only target:  base margin = 4.0 m
approaching:      margin *= 1 + 0.3 * min(approach_speed / 20, 1)
large object:     margin += 0.3 * max(length, width)
```

它的作用是把“协同感知目标是否可见”和“是否正在接近”变成实际约束半径。

在这个场景中：

- attacker 与 attacker_rear 都不是普通静态障碍物，而是高速参与者。
- 对接近中的 agent，安全区会被放大。
- 对遮挡或 V2X-only 目标，安全区更保守。

得到的结果是：后续路点检查不会只看“当前位置有没有重叠”，而是提前把未来几秒可能发生冲突的位置定义成危险区。

### 4.3 模块三：多 agent 几何路点修正

代码入口：

- 威胁查找：`coop_safety/learned/hybrid_safety.py:111`
- 多 agent 排斥：`coop_safety/learned/hybrid_safety.py:126`
- 主入口：`coop_safety/learned/hybrid_safety.py:230`

该模块对 10 个未来路点逐个检查：

```text
agent future position:
  a_i(t) = a_i(0) + v_i * ((t + 1) * 0.5)

if ||w_t - a_i(t)|| < margin_i:
  agent i is a threat to waypoint w_t
```

如果一个路点受到多个 agent 威胁，它不会只避最近的一个，而是把所有威胁 agent 的排斥方向加起来：

```text
F_t = sum_i force_i
w'_t = w_t + normalize(F_t) * push_distance
```

该模块在 `BS3_blind_stress` 中的直接结果是：

```text
Hybrid-thr0.30:        WPC = 0/2200
Hybrid+AND+TTC:        WPC = 0/2200
RearAware:             WPC = 0/2200
MinHarm:               WPC = 0/2200
RearEscape:            WPC = 0/2200
```

这说明几何路点修正已经成功把未来路点从 agent 的动态危险区中移出。也就是说，最终方法不是因为“路点还会撞但侥幸没撞”，而是在离散路点指标上已经达到 `0` waypoint collision。

但是，仅有这个模块还不够：

```text
Hybrid-GeomOnly:
  collision = 1
  first_collision = 49
  collision_pairs = attacker+ego
```

原因是：只修正局部路点但不形成有效闭环速度/横向动作时，车辆仍可能按原运动趋势进入路口冲突。因此需要后续检测和闭环控制模块。

### 4.4 模块四：V1 风险检测

代码入口：`coop_safety/learned/hybrid_safety.py:157`

V1 检测模块输入所有 agent 的相对状态，输出：

```text
collision_prob = P(collision)
```

在最终主线中，V1 不直接负责“如何修正路点”，而是提供一个风险触发信号。这个设计是检测-修正解耦：

```text
几何模块：负责修正未来路点
V1 模块：负责判断是否应该触发 warning/control
```

从消融结果看，V1-only 并不能解决本场景：

```text
Hybrid-V1Only-thr0.30:
  collision = 1
  first_collision = 24
  collision_pairs = attacker_rear+ego
  severity = 8.8695
  WPC = 22/2200
```

这说明只靠检测概率触发制动，会出现两个问题：

1. 没有几何修正，WPC 不为 0。
2. 制动策略仍会制造后车追尾。

因此 V1 的角色应理解为“风险信号”，不是完整安全策略。

### 4.5 模块五：AND 融合与 TTC 覆盖触发

代码入口：

- `HybridWithGeometricANDTTC`：`experiments/method_variants.py:81`
- TTC 计算：`coop_safety/learned/hybrid_safety.py:208`

该模块的逻辑是：

```text
fire = (P_v1 > 0.30 and n_geom > 0) or (min_ttc < 3.0)
```

它解决两个问题：

- `AND`：只有神经风险和几何威胁同时支持时才触发，降低普通 Hybrid 的误触发。
- `TTC override`：如果 TTC 已经很短，即使 V1 或几何信号滞后，也强制触发。

在本场景中，它的结果是：

```text
Hybrid+AND+TTC:
  collision = 1
  first_collision = 35
  collision_pairs = attacker_rear+ego
  first_warning = 27
  WPC = 0/2200
  warning_frames = 36
```

相比普通 Hybrid：

```text
Hybrid-thr0.30:
  first_collision = 19
  warning_frames = 57

Hybrid+AND+TTC:
  first_collision = 35
  warning_frames = 36
```

说明 AND+TTC 已经减少了过早/过多控制，并把后车追尾从 step 19 推迟到 step 35。但它仍然失败，因为它还没有区分“前方风险”和“后方风险”，仍然把风险主要映射成减速。

### 4.6 模块六：方向性 TTC 与后车风险识别

代码入口：

- `estimate_directional_ttc()`：`experiments/method_variants.py:104`
- `estimate_rear_gap()`：`experiments/method_variants.py:127`

这个模块把总体 TTC 拆成：

```text
front_side_ttc: 前方/侧方风险 TTC
rear_ttc:       后方风险 TTC
rear_gap:       同车道最近后车距离
```

区分规则是：

```text
if agent.x < -1.0 and abs(agent.y) < 3.5:
    this is rear same-lane risk
else:
    this is front/side risk
```

这个模块带来的关键变化是：系统不再把所有危险都理解成“应该刹车”。它能识别：

```text
前方有风险，但后车也很近；
如果此时急刹，后车风险可能比前方风险更快变成真实碰撞。
```

`RearAware` 使用了这个信息，但只做了速度因子的保守修正：

```text
RearAware:
  collision = 1
  first_collision = 35
  collision_pairs = attacker_rear+ego
  severity = 13.3888
```

说明仅仅“不要刹得太狠”还不够，必须给出横向逃逸动作。

### 4.7 模块七：MinHarm 最小伤害式速度策略

代码入口：

- 完整 MinHarm：`coop_safety/constraint/min_harm.py:20`
- 当前实验变体：`experiments/method_variants.py:173`

当前实验中的 MinHarm 不是完整多边形安全动作空间求解，而是面向后车压力的工程化速度策略：

```text
if rear_gap < 18m and front_ttc >= 1.5s:
    keep speed, avoid hard braking
elif rear_ttc < 2.5s and rear_ttc < front_ttc:
    do not reduce speed too much
```

在 `BS3_blind_stress` 中结果是：

```text
MinHarm:
  collision = 1
  first_collision = 46
  collision_pairs = attacker_rear+ego
  severity = 11.6033
  first_warning = 27
  warning_frames = 26
```

对比 AND+TTC：

```text
AND+TTC first_collision = 35, severity = 13.50
MinHarm first_collision = 46, severity = 11.60
```

这说明 MinHarm 起到了作用：它把碰撞延后，并降低了严重度。但它没有彻底避开后车，因为仍缺少明确的横向逃逸行为。

### 4.8 模块八：RearEscape 后车压力横向逃逸

代码入口：

- `HybridWithGeometricANDTTCRearEscape`：`experiments/method_variants.py:213`
- SUMO 横向执行：`experiments/run_modified_sumo_comparison.py:539`
- 闭环中读取 `lane_escape`：`experiments/run_modified_sumo_comparison.py:628`

RearEscape 的核心逻辑是：

```text
close_rear = rear_gap < 18.0 or rear_ttc < 2.5

if close_rear and front_ttc >= 1.0:
    lane_escape = 1
    target_speed_factor = 1.0
elif front_ttc < 1.0:
    lane_escape = close_rear
    target_speed_factor = 0.5
elif front_ttc < 3.0:
    target_speed_factor = 0.7
elif fire:
    target_speed_factor = 0.8
else:
    target_speed_factor = 1.0
```

这一步是本场景成功的关键。

它做出的决策不是“继续强刹”，而是：

```text
后车很近 + 前方冲突还不是 1s 内立即碰撞：
  不急刹；
  保持速度；
  发出 lane_escape = 1；
  在 SUMO 中尝试切到相邻安全车道。
```

最终结果：

```text
RearEscape:
  collision = 0
  first_collision = -1
  overlap = 0
  overlap_count = 0
  severity = 0.0
  first_warning = 26
  WPC = 0/2200
  warning_frames = 40
  frames = 220
```

这说明它同时完成了三件事：

1. 没有撞前方 attacker。
2. 没有被后方 attacker_rear 追尾。
3. 没有产生视觉 overlap 或二次碰撞。

### 4.9 模块九：SUMO 闭环控制执行

代码入口：`experiments/run_modified_sumo_comparison.py:590`

闭环执行模块把算法输出转成 SUMO 车辆动作：

```text
if target_speed_factor is set:
    target_speed = ego_speed * target_speed_factor
    traci.vehicle.setSpeed("ego", target_speed)

if lane_escape == 1:
    apply_rear_escape_lane()
```

其中 `apply_rear_escape_lane()` 会检查相邻车道是否有足够间隙：

```text
best_clearance > 25.0 m
```

如果满足，就让 ego 换到相邻安全车道。若横向逃逸失败，则退化为较温和速度控制。

在 `BS3_blind_stress` 中，RearEscape 的最终 `overlap=0` 和 `collision=0` 说明横向逃逸执行成功，算法输出不只是停留在风险分析层，而是通过闭环动作改变了真实碰撞结果。

## 5. 按时间线解释最终方法为什么成功

### 阶段一：未接近冲突前

ego 正常进入路口，attacker 和 blocker 构成前方/侧方潜在冲突，后车 `attacker_rear` 在 ego 后方接近。

感知模块输出：

```text
front/side agents exist
rear agent exists
visibility and relative velocity are available
```

### 阶段二：检测到前方风险

几何模块发现未来路点会接近 attacker 的动态危险区，因此修正路点。TTC 模块也发现前/侧向风险进入 `3s` 阈值。

输出：

```text
n_geom > 0
front_ttc < 3.0
WPC after correction = 0/2200
```

### 阶段三：识别到后车压力

方向性 TTC 模块发现后方同车道存在近距离/短 TTC 车辆：

```text
rear_gap < 18.0 or rear_ttc < 2.5
close_rear = True
```

这一步让系统知道：如果继续用强制减速处理前方风险，会把风险转移到后方。

### 阶段四：RearEscape 做出关键决策

因为：

```text
close_rear = True
front_ttc >= 1.0
```

RearEscape 输出：

```text
lane_escape = 1
target_speed_factor = 1.0
```

含义是：当前不是 1 秒内必撞的前方风险，所以不急刹，而是保持速度并横向逃逸。

### 阶段五：闭环执行改变真实结果

SUMO 执行层接收到：

```text
lane_escape = 1
```

然后尝试相邻车道换道。如果相邻车道有足够 clearance，则执行 `changeLane()`。最终 ego 没有和前方 attacker 发生碰撞，也没有被后方 attacker_rear 追尾。

最终结果：

```text
collision = 0
overlap = 0
severity = 0.0
WPC = 0/2200
first_warning = 26
```

## 6. 这个实例说明了什么

### 6.1 不是单个模块带来最好结果，而是模块组合带来最好结果

从消融可以看出：

- 只有 V1：会被后车追尾，且 WPC 不为 0。
- 只有几何：路点可以安全，但闭环动作不足，仍撞前方。
- 几何 + TTC：能及时响应，但仍把风险转化成追尾。
- RearAware：知道后车风险，但没有给出足够强的横向动作。
- MinHarm：能延后碰撞、降低严重度，但不能彻底避免。
- RearEscape：在几何安全、TTC 触发、后车识别基础上，加入横向逃逸，最终避免碰撞。

因此，最好结果来自：

```text
可见性/速度自适应安全区
+ 多 agent 几何路点修正
+ V1/AND/TTC 风险触发
+ 方向性后车风险识别
+ RearEscape 横向逃逸
+ SUMO 闭环执行
```

### 6.2 当前方法真正解决的问题

这个实例说明，当前方法解决的不是简单“预测会不会撞”，而是更具体的问题：

```text
在协同感知和遮挡场景中，
当前方风险和后方风险同时存在时，
如何生成不会把风险从前方转移到后方的安全约束？
```

这也是最终创新点中“方向性感知的最小伤害式闭环约束”的核心证据。

### 6.3 可以用于论文中的案例描述

论文中可以这样概括：

> In the BS3 blind stress case, the ego vehicle faces a corner conflict while an aggressive rear vehicle approaches from behind. Methods that react by braking avoid or delay the front conflict but are rear-ended: RSS collides at step 22, RiskMM at step 20, Hybrid at step 19, and AND+TTC at step 35. MinHarm delays the rear impact to step 46 and reduces severity, but still collides. Our RearEscape variant detects the rear pressure, suppresses hard braking when the front TTC is not below 1s, and activates a lateral escape. It achieves zero collision, zero overlap, and zero waypoint collision on this case.

中文表达可以是：

> 在 BS3 blind stress 场景中，ego 同时面对路口侧向攻击车和后方高速追车。传统强制减速方法虽然能响应前方风险，但会诱发后方追尾；MinHarm 能延后碰撞并降低严重度，但仍无法完全避免碰撞。最终 RearEscape 通过方向性 TTC 判断后车压力，在前方风险尚未进入 1s 内必撞状态时抑制急刹并触发横向逃逸，最终实现真实碰撞为 0、overlap 为 0、WPC 为 0/2200。

## 7. 与论文创新点的对应

| 模块 | 在实例中的作用 | 实例结果 | 对应创新点 |
|---|---|---|---|
| 协同/遮挡感知输入 | 提供 attacker、blocker、rear attacker 的相对状态和可见性 | 能同时看到前/后风险 | 协同感知驱动的安全约束 |
| 可见性自适应 margin | 为不可见/接近目标放大危险区 | 提前定义动态危险区 | 可见性自适应安全集 |
| 多 agent 几何修正 | 把未来路点推出所有威胁区域 | RearEscape WPC = 0/2200 | 多威胁合力约束 |
| V1 风险检测 | 提供风险概率触发信号 | 单独使用失败，但作为组合信号有价值 | 检测-修正解耦 |
| AND + TTC | 降低纯 Hybrid 过多触发，并保留短 TTC 强制触发 | first warning = 27 左右，WPC=0 | 低误报风险触发 |
| 方向性 TTC / rear gap | 区分前向风险和后向风险 | 发现急刹会制造追尾 | 多方向风险分析 |
| MinHarm | 延后碰撞并降低严重度 | severity 13.50 -> 11.60，但仍碰撞 | 最小伤害思想 |
| RearEscape | 保持速度并触发横向逃逸 | collision=0, overlap=0 | 方向性感知的最小伤害式闭环约束 |

## 8. 创新点在该实例中的具体作用与效果

本节把论文创新点和该案例中的具体模块行为对应起来，说明每个创新点不是概念性描述，而是在 `BS3_blind_stress` 中产生了可观察效果。

### 8.1 创新点一：检测-修正解耦

对应模块：

- 几何路点修正：`HybridSafetyConstraint.constrain_waypoints()`
- V1 风险检测：`_detect_with_v1()`
- AND/TTC 风险触发：`HybridWithGeometricANDTTC`

它做了什么：

```text
几何模块不等待 V1 报警，而是始终检查每个未来路点是否进入动态危险区；
V1/AND/TTC 只决定是否触发 warning 和速度控制；
因此，路点安全和报警触发被拆开处理。
```

在该实例中达到的效果：

```text
Hybrid-thr0.30:        WPC = 0/2200
Hybrid+AND+TTC:        WPC = 0/2200
RearEscape:            WPC = 0/2200
```

这说明路点修正模块已经独立保证未来路点不撞；同时 AND/TTC 把控制触发从普通 Hybrid 的 `warning_frames=57` 降到 `36`，最终 RearEscape 为 `40`，避免普通 Hybrid 过早、过多控制。

该创新点解决的问题：

```text
如果只靠 V1：
  Hybrid-V1Only 碰撞，且 WPC = 22/2200。

如果只靠几何：
  Hybrid-GeomOnly WPC = 0，但仍在 step 49 撞 attacker。

检测-修正解耦后：
  几何负责路点安全，V1/AND/TTC 负责风险触发，两者组合才形成有效安全策略。
```

### 8.2 创新点二：可见性和接近速度自适应安全约束

对应模块：

- `_get_safety_margin()`
- `_find_threats()`

它做了什么：

```text
visible target 使用 2.5m 基础安全半径；
V2X-only / invisible target 使用 4.0m 基础安全半径；
接近速度越大，危险区越大；
目标尺寸越大，危险区越大。
```

在该实例中达到的效果：

该场景有遮挡车 `blk_parallel`，还有高速交叉 attacker 和后方 `attacker_rear`。自适应安全半径使系统不是等到几何重叠才反应，而是在未来路点预测阶段提前生成危险区。

可观察结果是：

```text
RearEscape 的 WPC = 0/2200；
Hybrid+AND+TTC / RearAware / MinHarm 的 WPC 也都是 0/2200；
说明动态安全半径确实把未来路点推出了危险区。
```

该创新点解决的问题：

```text
固定安全距离无法区分：
  ego 直接可见目标；
  遮挡/V2X-only 目标；
  静止目标；
  高速接近目标。

自适应安全约束把这些差异转成不同大小的危险区，
更符合协同感知场景下的位置不确定性和接近风险。
```

### 8.3 创新点三：多 agent 合力式路点修正

对应模块：

- `_multi_agent_repulsion()`

它做了什么：

```text
对每个未来路点，不只选择最近威胁；
而是收集所有进入安全半径的 agent；
把所有威胁 agent 的排斥力求和；
再把路点沿合力方向推出危险区。
```

在该实例中达到的效果：

`BS3_blind_stress` 中至少同时存在前方/侧方 attacker、遮挡车、后方 attacker_rear。多 agent 合力避免了“只避前方 attacker，却把路点推向另一辆车”的问题。

可观察结果：

```text
Hybrid-thr0.30:
  WPC = 0/2200
  但 collision = 1，原因是后续速度策略导致追尾。

RearEscape:
  WPC = 0/2200
  collision = 0
  overlap = 0
```

这说明多 agent 几何修正解决的是“未来路点空间安全”问题；它为最终闭环成功提供了基础，但仍需要后续方向性控制处理真实动力学碰撞。

该创新点解决的问题：

```text
单目标避障在多车交互中容易出现局部修正错误；
多 agent 合力把所有威胁同时纳入一个统一安全场，
更适合协同感知带来的多目标输入。
```

### 8.4 创新点四：AND + TTC 的低误报即时触发

对应模块：

- `HybridWithGeometricANDTTC`
- `_compute_min_ttc()`

它做了什么：

```text
普通情况：
  V1 风险概率过阈值，并且几何威胁存在，才触发。

紧急情况：
  如果 min_ttc < 3.0s，即使 AND 条件不足，也强制触发。
```

在该实例中达到的效果：

相比普通 Hybrid：

```text
Hybrid-thr0.30:
  first_collision = 19
  warning_frames = 57
  collision_pairs = attacker_rear+ego

Hybrid+AND+TTC:
  first_collision = 35
  warning_frames = 36
  collision_pairs = attacker_rear+ego
```

AND+TTC 的效果是：减少过多控制，并把追尾从 step 19 延后到 step 35。但它没有最终解决问题，因为它仍主要把风险转化为减速，缺少后车压力下的横向逃逸。

该创新点解决的问题：

```text
纯 Hybrid 容易因为 V1 风险信号提前触发较多控制；
纯 AND 又可能漏掉 TTC 已经很短的危险；
AND+TTC 同时保留低误报和紧急触发能力。
```

### 8.5 创新点五：方向性感知风险分析

对应模块：

- `estimate_directional_ttc()`
- `estimate_rear_gap()`

它做了什么：

```text
把整体 TTC 拆成：
  front_side_ttc：前方/侧方冲突风险；
  rear_ttc：后方追尾风险；
  rear_gap：同车道后车距离。

不再把所有风险都映射成同一种“刹车更强”的动作。
```

在该实例中达到的效果：

方向性分析识别出：

```text
前方 attacker 有冲突；
但后方 attacker_rear 也很近；
如果强制制动，后向风险会更早变成真实碰撞。
```

RearAware 只使用方向性风险做速度修正，结果为：

```text
RearAware:
  first_collision = 35
  collision_pairs = attacker_rear+ego
  severity = 13.3888
```

这说明方向性风险分析本身能识别风险结构，但仅靠速度因子仍不足以彻底避开后车，需要和 RearEscape 联合。

该创新点解决的问题：

```text
传统 TTC/RSS 类策略通常把低 TTC 统一解释为制动；
但在有后车压力时，制动本身会制造碰撞；
方向性 TTC 让系统知道“风险来自哪里”，从而选择不同约束动作。
```

### 8.6 创新点六：最小伤害式 RearEscape 闭环约束

对应模块：

- `HybridWithGeometricANDTTCMinHarm`
- `HybridWithGeometricANDTTCRearEscape`
- `apply_rear_escape_lane()`

它做了什么：

MinHarm 模块先尝试减少后车压力下的伤害：

```text
后车过近且前方不是立即碰撞：
  避免强制急刹；
  尽量保持速度；
  降低后方追尾严重度。
```

在本场景中，MinHarm 的效果是：

```text
AND+TTC:
  first_collision = 35
  severity = 13.50

MinHarm:
  first_collision = 46
  severity = 11.60
```

这说明 MinHarm 已经把碰撞延后并降低严重度，但仍然发生碰撞。

RearEscape 在 MinHarm 思路上进一步加入横向逃逸：

```text
if close_rear and front_ttc >= 1.0:
    lane_escape = 1
    target_speed_factor = 1.0
```

在本场景中，RearEscape 的效果是：

```text
collision = 0
first_collision = -1
overlap = 0
overlap_count = 0
severity = 0.0
WPC = 0/2200
```

这是该案例中最关键的创新效果：系统没有把“安全”简化成强制刹车，而是在前后风险冲突时选择更低总体风险的横向逃逸。

该创新点解决的问题：

```text
前向风险和后向风险同时存在时，
局部最保守动作不一定全局最安全；
RearEscape 把后车压力转化为横向动作约束，
从而避免前方碰撞和后方追尾。
```

### 8.7 创新点七：可解释中间风险输出

对应模块：

- `stats` 输出字典
- `collision_prob`
- `n_geometric_threats`
- `min_ttc`
- `front_side_ttc`
- `rear_ttc`
- `rear_gap`
- `lane_escape`
- `target_speed_factor`

它做了什么：

每一帧不仅输出“改后的路点”，还输出中间风险原因：

```text
是否有几何威胁；
V1 风险概率是多少；
最小 TTC 是多少；
前方 TTC 和后方 TTC 分别是多少；
后车距离是否过近；
是否触发横向逃逸；
速度因子如何调整。
```

在该实例中达到的效果：

我们可以从结果中解释为什么不同方法失败：

```text
Hybrid-thr0.30:
  有风险触发，但没有后向风险处置，step 19 被追尾。

AND+TTC:
  控制更少，追尾推迟到 step 35，但仍失败。

MinHarm:
  降低严重度并推迟到 step 46，但仍失败。

RearEscape:
  触发 lane_escape，最终 collision = 0。
```

这使该方法不是黑箱输出，而能形成论文中的逐帧风险解释链。

## 9. 汇总：创新点到效果的案例闭环

| 创新点 | 对应模块 | 在 BS3_blind_stress 中做了什么 | 达到的效果 |
|---|---|---|---|
| 检测-修正解耦 | 几何修正 + V1/AND/TTC | 几何保证路点安全，检测只控制触发 | RearEscape WPC = 0/2200 |
| 可见性自适应安全集 | `_get_safety_margin()` | 对遮挡/V2X-only/接近目标扩大危险区 | 提前发现未来路点风险 |
| 多 agent 合力修正 | `_multi_agent_repulsion()` | 同时考虑 attacker、blocker、rear attacker 的几何威胁 | 避免单目标避障局部错误 |
| AND + TTC 触发 | `HybridWithGeometricANDTTC` | 减少过早触发，同时保留短 TTC 强制响应 | 追尾从 step 19 延后到 step 35 |
| 方向性风险分析 | `front_side_ttc/rear_ttc/rear_gap` | 区分前向冲突和后向追尾风险 | 识别“急刹会造成后车碰撞” |
| MinHarm | `HybridWithGeometricANDTTCMinHarm` | 后车压力下避免过强制动 | 碰撞从 step 35 延后到 step 46，severity 下降 |
| RearEscape | `lane_escape + apply_rear_escape_lane()` | 保持速度并横向逃逸 | collision=0, overlap=0, severity=0 |
| 可解释输出 | `stats` 风险字段 | 输出每帧风险来源和动作原因 | 可形成案例级风险分析链 |

这个案例可以概括为：

```text
几何约束解决“未来路点会不会进入危险区”；
AND+TTC 解决“什么时候应该触发控制”；
方向性 TTC 解决“风险来自前方还是后方”；
MinHarm 解决“不能盲目强刹”；
RearEscape 解决“前后风险冲突时如何真正脱离危险”。
```

## 10. 结论

`BS3_blind_stress` 说明了当前最好结果的本质：不是某个单独模块“检测得更准”，而是多个模块形成了完整的安全约束链。几何模块保证未来路点不落入动态危险区，V1/AND/TTC 保证风险触发不过早也不滞后，方向性 TTC 识别后车压力，RearEscape 把“急刹”改成“保持速度 + 横向逃逸”，最终在真实闭环仿真中把碰撞从 `1` 降到 `0`。
