# 260615 研究设计与最优方法框架图

> 本文件从“模块功能 + 创新点”的角度画出当前研究设计方案图，并给出当前综合最优方法 `Hybrid+AND+TTC+RearEscape-thr0.30` 的算法框架图。  
> 图中不强调神经网络结构细节，而强调问题定义、模块功能、创新点和结果输出。

## 图 1：研究设计方案图

该图回答：当前研究是如何从协同感知输入出发，逐层生成安全约束，并最终在离线数据集和闭环仿真中验证效果的。

```mermaid
flowchart TB
    A["研究问题\n协同感知下如何生成可插拔安全约束\n降低未来路点碰撞\n同时避免误报和急刹诱发二次风险"]

    B["输入层\nCooperative Perception\n- ego 状态\n- surrounding agents\n- visibility / V2X-only 标记\n- upstream waypoints"]

    C["风险表征层\n多粒度风险信息"]
    C1["几何风险\n未来路点 vs agent 预测位置"]
    C2["检测风险\nV1 collision probability"]
    C3["时间风险\nTTC / front-side TTC / rear TTC"]
    C4["方向风险\nrear gap / rear pressure"]
    C5["扩展接口\nRiskMap / RiskGraph / RiskEvents\n长期可行性 / MinHarm"]

    D["安全约束生成层"]
    D1["时序动态安全集\nS_t = R_t \\ union D_i(t)"]
    D2["可见性自适应 margin\nvisible: 2.5m\nV2X-only: 4.0m"]
    D3["接近速度自适应危险区\napproach speed 越高 margin 越大"]
    D4["多 agent 排斥场\n所有威胁合力修正 waypoint"]

    E["触发与控制层"]
    E1["检测-修正解耦\n几何修正始终执行\n检测只控制 warning/control"]
    E2["AND + TTC\n降低普通 Hybrid 过触发\n保留短 TTC 强制响应"]
    E3["方向性后车风险处理\n前向风险不等于一律急刹"]
    E4["RearEscape\n后车压力大且前方非立即碰撞时\n保持速度 + 横向逃逸"]

    F["输出层"]
    F1["corrected waypoints"]
    F2["warning / detection flag"]
    F3["target_speed_factor"]
    F4["lane_escape"]
    F5["可解释风险信号\np_coll, n_geom, min_ttc,\nfront_ttc, rear_ttc, rear_gap"]

    G["实验验证层"]
    G1["DeepAccident 离线验证\nWPC / Det / FA / Mod"]
    G2["SUMO base 闭环验证\n真实 CollRate / Sev / 2ndC"]
    G3["SUMO stress 闭环验证\n后车压力 / 二次风险"]
    G4["SUMO random-stress\n随机压力与鲁棒性"]

    H["当前综合最优结论\nHybrid+AND+TTC+RearEscape-thr0.30\nDeepAccident WPC=0.29%\nSUMO base CollRate=0%\nSUMO stress CollRate=25%\nBS3_blind_stress collision=0"]

    A --> B
    B --> C
    C --> C1
    C --> C2
    C --> C3
    C --> C4
    C --> C5

    C1 --> D
    C2 --> D
    C3 --> D
    C4 --> D
    C5 -. "完整接口能力\n非当前最优主路径" .-> D

    D --> D1
    D --> D2
    D --> D3
    D --> D4

    D1 --> E
    D2 --> E
    D3 --> E
    D4 --> E

    E --> E1
    E --> E2
    E --> E3
    E --> E4

    E1 --> F
    E2 --> F
    E3 --> F
    E4 --> F

    F --> F1
    F --> F2
    F --> F3
    F --> F4
    F --> F5

    F --> G
    G --> G1
    G --> G2
    G --> G3
    G --> G4
    G --> H

    classDef problem fill:#f8f3d4,stroke:#9b7b00,stroke-width:1px,color:#111;
    classDef input fill:#e7f0ff,stroke:#2c5aa0,stroke-width:1px,color:#111;
    classDef risk fill:#e9f7ef,stroke:#2e7d32,stroke-width:1px,color:#111;
    classDef constraint fill:#fff0e6,stroke:#c25b00,stroke-width:1px,color:#111;
    classDef control fill:#f2e9ff,stroke:#6a1b9a,stroke-width:1px,color:#111;
    classDef output fill:#e6fbff,stroke:#00838f,stroke-width:1px,color:#111;
    classDef eval fill:#f5f5f5,stroke:#555,stroke-width:1px,color:#111;
    classDef best fill:#ffe8e8,stroke:#b71c1c,stroke-width:2px,color:#111;

    class A problem;
    class B input;
    class C,C1,C2,C3,C4,C5 risk;
    class D,D1,D2,D3,D4 constraint;
    class E,E1,E2,E3,E4 control;
    class F,F1,F2,F3,F4,F5 output;
    class G,G1,G2,G3,G4 eval;
    class H best;
```

### 图 1 对应的创新点

| 创新点 | 图中位置 | 核心作用 |
|---|---|---|
| 协同感知驱动的时序安全约束 | 输入层 + 安全约束生成层 | 把 V2X 感知结果转成未来路点安全集，而不是只输出碰撞概率 |
| 可见性自适应动态安全区 | `D2` | 对 V2X-only / invisible 目标使用更大 margin |
| 接近速度自适应危险区 | `D3` | 高速接近目标生成更大动态危险区 |
| 多 agent 排斥场 | `D4` | 同时考虑所有威胁目标，避免只避一个目标 |
| 检测-修正解耦 | `E1` | 几何修正保证 WPC，检测信号控制 warning/速度 |
| AND + TTC 触发 | `E2` | 降低过触发，同时保留紧急 TTC 触发 |
| 方向性感知最小伤害式控制 | `E3` + `E4` | 区分前向风险和后向风险，避免急刹诱发追尾 |
| 可解释风险输出 | `F5` | 输出风险来源和动作原因，支持案例分析和论文解释 |

## 图 2：当前最优方法算法框架图

该图只画当前综合最优方法 `Hybrid+AND+TTC+RearEscape-thr0.30` 的实际执行路径。

```mermaid
flowchart LR
    A["输入\nPerceptionResult + Waypoints\n- ego state\n- agents\n- is_visible\n- vx/vy/size\n- 10 future waypoints"]

    B["V1 风险检测\n_detect_with_v1()\n输出 p_coll"]

    C["几何安全约束\n_find_threats()\n_get_safety_margin()\n_multi_agent_repulsion()"]

    C1["可见性 margin\nvisible=2.5m\ninvisible=4.0m"]
    C2["接近速度放大\napproach speed 越大\nmargin 越大"]
    C3["多 agent 合力\n所有 threats 求合力\n修正 waypoints"]
    C4["后处理\ntrajectory smoothing\nspeed clamping"]

    D["TTC 风险\n_compute_min_ttc()\nmin_ttc"]

    E["AND + TTC 触发\nfire = (p_coll > 0.30 and n_geom > 0)\n       or (min_ttc < 3.0)"]

    F["方向性风险分析\nestimate_directional_ttc()\nestimate_rear_gap()"]

    F1["front_side_ttc\n前方/侧方冲突"]
    F2["rear_ttc\n后向追尾风险"]
    F3["rear_gap\n同车道最近后车距离"]

    G["RearEscape 决策"]
    G1["close_rear = rear_gap < 18m\nor rear_ttc < 2.5s"]
    G2["if close_rear and front_ttc >= 1.0s\nlane_escape=1\ntarget_speed_factor=1.0"]
    G3["elif front_ttc < 1.0s\ntarget_speed_factor=0.5"]
    G4["elif front_ttc < 3.0s\ntarget_speed_factor=0.7"]
    G5["elif fire\ntarget_speed_factor=0.8"]

    H["输出 stats + corrected waypoints"]
    H1["modified waypoints"]
    H2["n_collisions_detected"]
    H3["target_speed_factor"]
    H4["lane_escape"]
    H5["解释信号\np_coll, n_geom,\nfront_ttc, rear_ttc, rear_gap"]

    I["SUMO 闭环执行"]
    I1["速度控制\nsetSpeed(ego_speed * factor)"]
    I2["横向逃逸\napply_rear_escape_lane()\n相邻车道 clearance > 25m 则换道"]
    I3["路点横向意图\napply_waypoint_lane_intent()"]

    J["最终效果\nBS3_blind_stress:\ncollision=0\noverlap=0\nseverity=0\nWPC=0/2200\nfirst_warning=26"]

    A --> B
    A --> C
    A --> D
    A --> F

    C --> C1
    C --> C2
    C --> C3
    C --> C4
    C4 --> E

    B --> E
    D --> E
    F --> F1
    F --> F2
    F --> F3

    E --> G
    F1 --> G
    F2 --> G
    F3 --> G

    G --> G1
    G1 --> G2
    G1 --> G3
    G1 --> G4
    G1 --> G5

    G2 --> H
    G3 --> H
    G4 --> H
    G5 --> H
    C4 --> H

    H --> H1
    H --> H2
    H --> H3
    H --> H4
    H --> H5

    H1 --> I3
    H3 --> I1
    H4 --> I2
    I1 --> J
    I2 --> J
    I3 --> J

    classDef input fill:#e7f0ff,stroke:#2c5aa0,stroke-width:1px,color:#111;
    classDef neural fill:#f2e9ff,stroke:#6a1b9a,stroke-width:1px,color:#111;
    classDef geom fill:#fff0e6,stroke:#c25b00,stroke-width:1px,color:#111;
    classDef ttc fill:#e9f7ef,stroke:#2e7d32,stroke-width:1px,color:#111;
    classDef decision fill:#fff7d6,stroke:#9b7b00,stroke-width:1px,color:#111;
    classDef output fill:#e6fbff,stroke:#00838f,stroke-width:1px,color:#111;
    classDef exec fill:#f5f5f5,stroke:#555,stroke-width:1px,color:#111;
    classDef result fill:#ffe8e8,stroke:#b71c1c,stroke-width:2px,color:#111;

    class A input;
    class B neural;
    class C,C1,C2,C3,C4 geom;
    class D,F,F1,F2,F3 ttc;
    class E,G,G1,G2,G3,G4,G5 decision;
    class H,H1,H2,H3,H4,H5 output;
    class I,I1,I2,I3 exec;
    class J result;
```

### 图 2 的算法流程说明

1. 输入是统一的 `PerceptionResult` 和上游规划器未来路点。
2. V1 只输出 `p_coll`，不直接决定路点如何移动。
3. 几何模块始终执行，基于可见性、接近速度和多 agent 威胁修正未来路点。
4. TTC 模块提供 `min_ttc`，用于紧急覆盖触发。
5. AND+TTC 产生风险触发信号，降低普通 Hybrid 的过多 warning。
6. 方向性 TTC 把风险拆成 `front_side_ttc` 和 `rear_ttc`，再结合 `rear_gap` 判断是否存在后车压力。
7. RearEscape 在后车压力大且前方非 1s 内立即碰撞时，不急刹，而是输出 `lane_escape=1` 和 `target_speed_factor=1.0`。
8. SUMO 执行层把速度因子和横向逃逸意图转成真实闭环动作。

## 图 3：最优方法的简化论文版框架

如果论文或 PPT 空间有限，可以使用下面这个压缩版。

```mermaid
flowchart TB
    A["Cooperative Perception + Planner Waypoints"]

    B["Geometric Safety Constraint\nvisibility-aware margin\napproach-speed margin\nmulti-agent repulsion"]
    C["Risk Trigger\nV1 probability\nAND fusion\nTTC override"]
    D["Directional Risk Reasoning\nfront/side TTC\nrear TTC\nrear gap"]

    E["RearEscape Decision\nif rear pressure and front risk not immediate:\nkeep speed + lateral escape\nelse: speed reduction"]

    F["Safe Outputs\ncorrected waypoints\nspeed factor\nlane escape\nrisk explanation"]

    G["Closed-loop Execution\nspeed control + lane change"]
    H["Observed Result\nlower WPC\nlower collision rate\navoid rear-end induced by hard braking"]

    A --> B
    A --> C
    A --> D
    B --> F
    C --> E
    D --> E
    E --> F
    F --> G
    G --> H

    classDef core fill:#fff0e6,stroke:#c25b00,stroke-width:1px,color:#111;
    classDef risk fill:#e9f7ef,stroke:#2e7d32,stroke-width:1px,color:#111;
    classDef decision fill:#f2e9ff,stroke:#6a1b9a,stroke-width:1px,color:#111;
    classDef result fill:#ffe8e8,stroke:#b71c1c,stroke-width:2px,color:#111;
    class B core;
    class C,D risk;
    class E,F decision;
    class H result;
```

## 可直接配图说明

论文中可以这样描述图 1：

> The research design formulates cooperative driving safety as a constraint generation problem. Cooperative perception with visibility metadata is converted into multi-granularity risk signals, which are then transformed into time-indexed waypoint safety sets and directional closed-loop constraints. The final output contains corrected waypoints, speed constraints, lateral escape intent, and interpretable risk signals.

论文中可以这样描述图 2：

> The final method, Hybrid+AND+TTC+RearEscape, decouples geometric waypoint correction from risk-triggered control. The geometric branch always enforces visibility-aware multi-agent waypoint safety, while the trigger branch combines V1 probability, geometric threats, and TTC. Directional TTC and rear-gap reasoning detect rear pressure; when rear risk dominates and the front conflict is not immediate, RearEscape suppresses hard braking and activates lateral escape.
